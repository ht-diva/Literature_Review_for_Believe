---
# common parameters
n_cores: &cores 4

# This is an ordered dict that contains the order in which the steps are run.
run_sequence: !!omap
  - 1: 'basic_check'
  - 2: 'infer_build'
  - 3: 'harmonize'
  - 4: 'sort_alphabetically'
  - 5: 'write_tsv'


steps:
  basic_check: # see https://cloufield.github.io/gwaslab/Standardization/
    params:
      run: True
    gl_params:
      threads: *cores
      normalize: True
      fix_id_kwargs:
        fixid: True
        overwrite: True
  infer_build: # see https://cloufield.github.io/gwaslab/InferBuild/
    params:
      run: true
  harmonize: # see https://cloufield.github.io/gwaslab/Harmonization/
    params:
      run: True
    gl_params:
      basic_check: False
      threads: *cores
  sort_alphabetically:
    params:
      run: True
    gl_params:
      threads: *cores
  write_tsv:
    params:
      run: True
      workspace_subfolder: False
    gl_params:
      fmt: "gwaslab"
      xymt_number: True
      to_csvargs: {}


# Filename transformation, e.g. pqtl_sun.tsv
filename_mask: [ True, False]
filename_sep: '.'

# IO
root_path: "results"
log_filename: "gwaspipe.log"
formatbook_path: "formatbook.json"